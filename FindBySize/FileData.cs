namespace FindBySize
{
    public class FileData
    {
        public string Name;
        public long Length;

        public FileData(string name, long length) { Name = name; Length = length; }
    }
}